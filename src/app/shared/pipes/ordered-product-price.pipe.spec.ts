import { OrderedProductPricePipe } from './ordered-product-price.pipe';

describe('OrderedProductPricePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderedProductPricePipe();
    expect(pipe).toBeTruthy();
  });
});
