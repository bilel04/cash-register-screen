import { TotalQuantityPricePipe } from './total-quantity-price.pipe';

describe('TotalQuantityPricePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalQuantityPricePipe();
    expect(pipe).toBeTruthy();
  });
});
